{
  "name": "docker-test",
  "version": "1.0.0",
  "description": "A simple Node.js server for Docker testing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}